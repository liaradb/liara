services:
  liaradb:
    ports:
      - 50055:50055
    image: liaradb
    build:
      dockerfile: Dockerfile
      context: .
      additional_contexts:
        eventsource_go: ../../modules/eventsource_go
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./.dbdata:/app/.dbdata
